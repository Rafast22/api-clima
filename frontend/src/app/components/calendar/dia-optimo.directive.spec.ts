import { DiaOptimoDirective } from './dia-optimo.directive';

describe('DiaOptimoDirective', () => {
  it('should create an instance', () => {
    const directive = new DiaOptimoDirective();
    expect(directive).toBeTruthy();
  });
});
